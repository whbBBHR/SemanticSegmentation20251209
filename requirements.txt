# Compatible with Python 3.9+
torch>=1.13.0,<2.0.0
torchvision>=0.14.0,<0.15.0
numpy>=1.21.0,<2.0.0
opencv-python>=4.5.0
Pillow>=9.0.0
tqdm>=4.62.0
albumentations>=1.3.0
matplotlib>=3.5.0
scikit-learn>=1.0.0

# For optimization (macOS compatible)
onnx>=1.12.0,<1.15.0
onnxruntime>=1.12.0,<1.15.0  # CPU version for macOS
thop>=0.1.1

# Claude AI integration (optional)
anthropic>=0.18.0

# Optional for visualization
# tensorboard>=2.11.0
# wandb>=0.13.0

# Note: TensorRT requires NVIDIA GPU and is not available on macOS
# For GPU acceleration on macOS, use Metal Performance Shaders (MPS) with PyTorch
